/* Pre-processors commands: Include libraries. */
#include <stdio.h>

/* Main function, all code is written here. */
int main()
{
  /* printf: function that print on terminal a message  */
  printf("Hello World!");

  /* Exit program */
  return 0;
}
